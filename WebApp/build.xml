<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003"
         DefaultTargets="BuildProjects"
         ToolsVersion="4.0">

  <Import Project="$(MSBuildExtensionsPath)\MSBuildCommunityTasks\MSBuild.Community.Tasks.Targets"/>

  <UsingTask Condition=" '$(TEAMCITY_VERSION)' != '' "
              TaskName="NUnitTeamCity"
              AssemblyFile="$(teamcity_dotnet_nunitlauncher_msbuild_task)" />

  <PropertyGroup>
    <Configuration Condition=" $(Configuration) == '' ">Debug</Configuration>
  </PropertyGroup>

  <ItemGroup>
    <BuildProjects Include="JumpersForGoalPosts.sln"/>
  </ItemGroup>

  <Target Name="BuildProjects">
    <MSBuild Projects="@(BuildProjects)"
             Targets="Build" />
  </Target>
</Project>